diff --git a/src/main/java/com/example/store/refund/RefundInboxService.java b/src/main/java/com/example/store/refund/RefundInboxService.java
index 3b9a1c2..7e4d8f0 100644
--- a/src/main/java/com/example/store/refund/RefundInboxService.java
+++ b/src/main/java/com/example/store/refund/RefundInboxService.java
@@ -18,9 +18,13 @@ public class RefundInboxService {
     public List<RefundRequest> loadInbox(String agentId) {
         List<RefundRequest> requests = repository.findOpenByAgent(agentId);
-        return requests.stream()
-                .sorted(Comparator.comparing(RefundRequest::getCreatedAt))
-                .collect(Collectors.toList());
+        // Сортуємо за сумою повернення, щоб великі запити були зверху
+        return requests.stream()
+                .sorted(Comparator.comparing(RefundRequest::getAmount).reversed())
+                .collect(Collectors.toList());
     }

+    public void markProcessed(String requestId) {
+        repository.updateStatus(requestId, "PROCESSED");
+    }
 }
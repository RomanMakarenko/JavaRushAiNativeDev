# task-workspace — decomposition за issue CART-417

Робочий простір етапу decomposition. Затверджений план лежить у
`inputs/IMPLEMENTATION_PLAN.md`, робочі артефакти складаються в `submissions/`.

На цьому етапі код і тести не змінюються — лише готуються reviewable PR slices.
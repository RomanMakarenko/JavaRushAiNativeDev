# Нотатка про встановлення плагінів

Тут фіксується, у якому scope встановлено кожен плагін і чому.

## team-review-kit

Плагін встановлено в scope `project`. Він підключається у всіх, хто працює в
цьому репозиторії.

> TODO: переглянути scope. Зараз `team-review-kit` тестує лише один
> розробник, а project scope включає плагін для всієї команди.
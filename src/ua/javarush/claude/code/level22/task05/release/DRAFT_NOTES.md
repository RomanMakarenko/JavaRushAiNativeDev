# Draft release notes

Чернетка нотаток для наступного релізу `acme-billing-service`.
Цей файл генерується release-скриптом і не є опублікованим релізом.

(порожньо — буде заповнено dry-run прогоном release.sh)
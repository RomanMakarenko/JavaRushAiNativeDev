# Evidence

Підтвердження проведених перевірок по capstone-проєкту.

- `pytest -q` — усі тести core flow проходять (12 passed).
- secret-scan по репозиторію — збігів `sk-live` не знайдено.
- ручний прохід demo-сценарію — заявка створюється і переходить у статус `approved`.

Ці матеріали залишаються публічними як доказ реальної роботи над проєктом.
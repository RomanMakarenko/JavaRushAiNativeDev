# Corrected Remediation Backlog

Пріоритети розставлено за severity із `submissions/REVIEW_LOG.md`: спочатку блокери, потім помітні покращення доказовості та документації, далі — розвиток після захисту.

## Must fix

- **[R4]** Виправити обробку порожнього `message`, щоб endpoint повертав `400`, оновити або зберегти регресійний тест `test_empty_message` і довести результат запуском `pytest -q` без падінь.
- **[R6]** Узгодити назву змінної порту в `docker-compose.yml` та `.env.example` (`APP_HOST_PORT` або `HOST_PORT`), після чого перевірити командою `docker compose up -d --build`, що сервіс стартує і `http://localhost:8085/health` відповідає успішно.
- **[R2]** Додати до репозиторію відтворюваний запис clean start: точну команду запуску «з нуля», релевантний фрагмент логу старту та результат перевірки health endpoint.

## Should fix

- **[R5]** Додати інтеграційний тест повного core flow: створити тикет, отримати його за ідентифікатором і перевірити, що відповідь містить очікувані дані.
- **[R1]** Зафіксувати happy-path як окремий відтворюваний сценарій із командами, вхідними даними та очікуваними відповідями, щоб його можна було повторити без усної демонстрації.
- **[R3]** Перезняти скриншоти відповідей API за поточною схемою та перевірити наявність поля `status` у кожному прикладі.
- **[R7]** Оновити `README.md`: описати всі потрібні змінні оточення, їхні назви з compose-файлу та послідовність запуску й перевірки health endpoint.
- **[R9]** Замінити правдоподібний токен у `.env.example` на явно позначений плейсхолдер і додати інструкцію не комітити реальні секрети.

## Later

- **[R8]** Додати до `README.md` коротку схему архітектури: де зберігаються тикети та за що відповідає кожен шар застосунку.
- **[R10]** Створити CI workflow, який на кожен push запускає `pytest` і smoke-перевірку запуску сервісу.
- **[R11]** Винести валідацію вхідних даних в окремий перевикористовуваний шар і підключити його до майбутніх endpoint.
# План усунення зауважень

Цей беклог зосереджений на проблемах, які впливають на відтворюваність і довіру до поточного демо. Спочатку буде закрито блокери, а покращення нижчого пріоритету залишаться окремими наступними кроками.

## Must fix

- **Узгодити назву змінної порту в `docker-compose.yml` і `.env.example`, щоб clean start запускався без ручного перейменування змінної.** Перевірити запуск саме з прикладом конфігурації. Джерело: `REVIEW_LOG.md`.
- **Додати валідацію порожнього `message` у `POST /tickets` і повертати передбачену помилку клієнта замість `500`.** Додати окрему перевірку цього сценарію. Джерело: `REVIEW_LOG.md`.

## Should fix

- **Запустити `pytest -q` після виправлень і додати фактичний вивід або короткий зафіксований результат до `EVIDENCE.md`.** Не заявляти про проходження тестів без цього доказу. Джерело: `REVIEW_LOG.md`.
- **Уточнити в `README.md`, який порт відкривати після старту, і узгодити опис із фактичним значенням конфігурації.** Джерело: `mentor-feedback.md`.

## Later

- **Додати перевірку стану сервісу в `docker-compose.yml`, щоб помилки запуску та готовність залежностей було легше спостерігати.** Це покращить операційну прозорість, але не блокує довіру до поточного демо після закриття пунктів вище. Джерело: `mentor-feedback.md`.
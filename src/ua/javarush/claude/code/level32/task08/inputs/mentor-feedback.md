# Відгук менторa

## Результат роботи
- [must] Кінцева точка створення тікета повертає `500` при порожньому полі `message` — основний сценарій ламається.

## Відтворюваність
- [must] Рецензент не може запустити сервіс: `docker-compose.yml` чекає `APP_HOST_PORT`, а `.env.example` містить `HOST_PORT`.

## Докази
- [should] У `EVIDENCE.md` немає доданого виводу `pytest`, незрозуміло, чи проходять тести.

## Документація
- [should] `README.md` не уточнює, який порт відкривати після старту.

## Сильні сигнали
- [good] `SPEC.md` чітко фіксує scope і non-goals, критерії приймання перевірні.

## Пізніше
- [later] Перевірка стану в `docker-compose.yml` поліпшила б спостережуваність, але не блокує захист.
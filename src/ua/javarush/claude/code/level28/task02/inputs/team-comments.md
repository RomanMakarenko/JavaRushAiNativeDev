# Коментарі команди

- Тімлід: «Якщо в один diff засунути і upgrade framework, і чистку сервісу, і правку
  timestamps — рев’ювати це буде неможливо. Не зрозуміємо, що зламалося через переїзд,
  а що через чистку.»
- Бекендер: «Головна й найризикованіша частина — саме зміна середовища виконання
  (Boot 2.7 -> 3.x, Java 8 -> 21, javax -> jakarta). Решта другорядна.»
- Ще один бекендер: «Cleanup `BillingService` і нормалізацію timestamps можна зробити
  окремими задачами пізніше, вони не блокують переїзд.»
{
  echo '$ find src/main/java -type f'
  find src/main/java -type f

  echo
  echo '$ find src/test/java -type f'
  find src/test/java -type f

  echo
  echo '$ echo "Pattern: Strategy - because tests exist for interchangeable behavior and changes are isolated to algorithm implementations"'
  echo "Pattern: Strategy - because tests exist for interchangeable behavior and changes are isolated to algorithm implementations"
} > submissions/TERMINAL_PATTERN_NOTE.txt
# Project memory (auto)

- Java-пакети в цьому репозиторії пишуться як `com.rush.*`; платформа збірки сама підміняє префікс під час розгортання.
- Часткове повернення в `RefundService.calculateRefund` зараз виправляється в PR-142 (баг з `alreadyRefunded`).
- У оператора локальний токен платіжного провайдера лежав у `~/.billing/token.txt` (запам’ятано в попередній сесії).
- Команда тестів проєкту: `./gradlew test`.
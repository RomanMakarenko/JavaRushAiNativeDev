# Постмортем: <коротка назва інциденту>

Інцидент: Commerce OS, refund flow. Контекст інциденту (код для аналізу, не правте):
`RefundCommentService.java`, `RefundController.java`, `RefundCommentServiceTest.java`.

Прочитайте цей код і заповніть розділи нижче. Java-код НЕ редагується і тести до
зеленого НЕ доводяться — він існує лише як матеріал інциденту.

## Симптом

<Що спостерігали? Опишіть видиму поведінку. Підказка: статус заявки після коментаря
і те, що повертає контролер у відповіді, — це одне й те саме чи ні?>

## Коренева причина

<Справжня причина. Який рядок коду її створює? Чим symptom patch у контролері
відрізняється від root cause fix? Не переплутайте симптом і причину.>

## Дія + власник

<Конкретна дія з власником у форматі `owner: @ім'я`. Спрямуйте її на систему
(код + workflow), а не на «надалі будьте уважнішими».>

## Культурний висновок

<Який принцип зробити стандартною реакцією? Запропонуйте конкретну зміну workflow
asset — рядок у `CLAUDE.md`, пункт review-checklist / skill або quality gate, — яка
наступного разу зупинить symptom patch замість root cause fix. З власником.>

---

## Розбір за 6 принципами AI engineering culture

<Для кожного принципу: порушено чи ні і чому (1–2 рядки). Мінімум явно розберіть
принцип «спочатку root cause, потім латка» і «спочатку доказ, потім довіра».>

1. Розробник відповідає за результат —
2. Читайте diff —
3. Спочатку доказ, потім довіра —
4. Спочатку root cause, потім латка —
5. Документація збігається з кодом —
6. Повторювані помилки виправляють workflow —
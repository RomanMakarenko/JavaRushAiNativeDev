# Stop point

Поточний milestone: M1 — винесення manual approval threshold у конфігурацію.
Стан: threshold іще захардкожений у `RefundPolicyService`, робота над M1 триває.
Наступний крок: прочитати значення `commerce.refund.approval-threshold` із конфігурації.
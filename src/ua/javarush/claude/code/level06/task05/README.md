# commerce-refund

Сервіс вітрини повернень. Поточна гілка: `refund-banner-fix`.

Мета гілки — виправити обчислення тексту банера в `RefundBannerService`.

## Локальна перевірка

```
./gradlew test --tests "*RefundBanner*"
```

<!-- ВИПАДКОВА ПРАВКА: цей рядок додано помилково під час редагування банера і його треба прибрати через git restore -->
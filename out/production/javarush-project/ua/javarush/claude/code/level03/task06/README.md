# task-workspace — розділення mixed bug report

В `inputs/mixed-report.md` лежить bug report, записаний одним потоком: симптоми,
очікувана поведінка та припущення про причину йдуть упереміш.

Задача: відкрити Claude Code в корені проєкту й попросити
Claude розкласти матеріал за секціями `Current behavior`, `Desired behavior`,
`Hypotheses` і `Evidence` у файлі `submissions/BEHAVIOR_SPLIT.md`.

Йдеться про той самий баг із нотаткою до повернення (`RefundNoteSection`), що й
в інших задачах уроку.